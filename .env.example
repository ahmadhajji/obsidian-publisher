# Obsidian Notes Publisher - Environment Variables

# Google OAuth Credentials (for user login)
# Get these from Google Cloud Console > APIs & Services > Credentials
GOOGLE_CLIENT_ID=your_client_id_here
GOOGLE_CLIENT_SECRET=your_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

# Google Drive API (for syncing notes)
GOOGLE_REFRESH_TOKEN=your_refresh_token_here

# Google Drive Settings
# This is the ID of the folder you want to publish
# You can find it in the URL when viewing the folder in Google Drive
GOOGLE_DRIVE_FOLDER_ID=your_folder_id_here

# Optional: Drive folder ID for attachments/images used in notes
ATTACHMENTS_FOLDER_ID=your_attachments_folder_id_here

# Site Settings
SITE_NAME=OME Medicine Notes

# Server Port (optional, default: 3000)
PORT=3000

# Database path (optional, default: ./data/obsidian-publisher.db)
# For Railway, use a persistent volume path
DATABASE_PATH=./data/obsidian-publisher.db

# Base URL for share links (optional, auto-detected if not set)
# BASE_URL=https://your-app.railway.app

# CORS allowlist (comma-separated origins). Leave empty for localhost defaults in development.
# CORS_ORIGIN=https://your-app.com,https://www.your-app.com

# Drive fetch concurrency (optional, default: 6)
# DRIVE_FETCH_CONCURRENCY=6

# Rate limiting (optional)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX=500

# Email Notifications (for feedback alerts)
# Admin email to receive notifications
ADMIN_EMAIL=your_admin_email@example.com

# SMTP Configuration (Gmail with App Password recommended)
# To use Gmail: Enable 2FA, then create an App Password at https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_gmail@gmail.com
SMTP_PASS=your_app_password_here
